// To check for Armstrong [magic] number.
//
#include<stdio.h>
int main(){
  int num,x,a=0,num2;
  printf("enter a number to check if its armstrong number or not: ");
  scanf("%d",&num);
  x=num;
  do{
      x=num/10;
      a++;
      num=x
    }
  }while(x>0);
for(int i=1;i<=a;i++){
  num2=num%10;
  x=num/10
  num=x
}
}
